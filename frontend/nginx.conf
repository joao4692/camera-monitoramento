server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # Rotas do Angular (ex: /admin/login) não são arquivos reais no disco —
  # sem isso, dar F5 numa rota interna resultaria em 404 do Nginx.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
